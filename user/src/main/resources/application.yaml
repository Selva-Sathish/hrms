spring:
  application:
    name: user
  datasource:
    url: jdbc:postgresql://postgres:5432/hrms
    username: admin
    password: admin123
  jpa:
    hibernate:
      properties:
        ddl-auto: create-drop
      show_sql: true