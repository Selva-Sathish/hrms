spring:
  application:
    name: user
  datasource:
    url: jdbc:postgresql://postgres:5432/hrms
    username: admin
    password: admin123
  jpa:
    hibernate:
      ddl-auto: create-drop
    show_sql: true
  security:
    oauth2:
      resourceserver:
        jwt:
          public-key-location: ${JWT_PUBLIC_KEY_PATH}