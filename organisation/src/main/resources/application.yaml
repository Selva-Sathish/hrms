spring:
  application:
    name: organisation
  datasource:
    url: jdbc:postgresql://postgres:5432/hrms
    username: admin
    password: admin123
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
jwt:
  public-key-path: ${JWT_PUBLIC_KEY_PATH}